{% extends "body.j2" %}{% block content %}
<div id="simulation-container">
    <img id="placa-image" src="data:image/png;base64,{{ image_base64 }}" alt="Distribución de Temperatura">
</div>

<div id="controls">
    <h3>Controles del Soplete</h3>
    <label for="temperatura">Temperatura (°C):</label>
    <input type="range" id="temperatura" name="temperatura" min=0 max="500" value={{soplete.temperatura}} oninput="updateTempValue(this.value)" required>
    <span id="temp-value">{{soplete.temperatura}} °C</span>
    <button id="start">Aplicar Soplete</button>
    <h3>Resultados</h3>
    {# <p class="hot">Posición del punto caliente: 
    {% for temp in placa.temperatura %}
     <span id="hot-spot">{{temp}}</span>
        
    {% endfor %}
    </p> #}
</div>

{% endblock %}

